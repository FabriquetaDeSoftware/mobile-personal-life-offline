import { StyleSheet } from 'react-native';

export const s = StyleSheet.create({
  container: {
    height: 'auto',
  },
  content: {
    gap: 8,
    alignItems: 'center',
    justifyContent: 'space-between',
    width: '100%',
  },
});
